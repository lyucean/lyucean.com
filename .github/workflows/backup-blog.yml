name: Backup Blog

on:
  schedule:
    - cron: '0 3 * * *'
  push:
    paths:
      - ".github/workflows/backup-blog.yml"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  backup-db:
    name: "Бекап БД"
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: "Подготовка репозитория"
        uses: actions/checkout@v4

      - name: "Снимем дамп БД"
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script_stop: true
          script: |
            cd /var/www/lyucean.com/
            make backup-db

  backup-file:
    name: "Бекап файлов"
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: "Подготовка репозитория"
        uses: actions/checkout@v4

      - name: "Создаём архив"
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script_stop: true
          script: |
            cd /var/www/lyucean.com/
            make backup-file

  backup-prune:
    name: "Ротация бэкапов"
    runs-on: ubuntu-latest
    environment: production
    needs: [ backup-file, backup-db ]
    steps:
      - name: "Подготовка репозитория"
        uses: actions/checkout@v4

      - name: "Удалим бэкапы старше 7 дней"
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script_stop: true
          script: |
            cd /var/www/lyucean.com/
            make backup-prune

  alert:
    name: "Уведомление"
    runs-on: ubuntu-latest
    needs: [ backup-file, backup-db, backup-prune ]
    steps:
      - name: "PingZen heartbeat"
        run: curl -fsS -X POST --connect-timeout 15 --max-time 30 "https://pingzen.dev/api/v1/ping/eHu7lT5s-H8CaydTWt6SiQ/lyuceancom-backup" >/dev/null

      - name: "Отправим сообщение через телеграм"
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: |
            Бекап проекта ${{ github.repository }} сделан. Хранение 7 дней.
